# KNOWN_ISSUES

更新时间：2026-05-07

## 当前阻塞与已知问题

### Git index.lock 阻塞（已解决 ✅ 2026-05-07）

- 已通过 Python 文件覆盖和 git plumbing 命令绕过沙箱限制。
- 合并冲突已清理，4 个新提交已创建，工作区干净。
- 详见下方 GitHub 上传阻塞。

### GitHub 上传暂时阻塞

- 现象：`git pull` / `git push` 访问 `https://github.com/jezljjq/H5.git` 失败。
- 错误：无法连接 `github.com:443`。
- 当前状态：必须先解决 index.lock 问题才能考虑上传。
- 不要强推，不要反复无意义重试。

### 默认任务模板不完整

- `config/app_config.json` 当前引用 32 个唯一模板。
- `assets/templates` 当前实际只有：
  - `神界大陆.bmp`
  - `111.png`
- `神界大陆.bmp` 被配置引用。
- `111.png` 未被配置引用。
- 当前默认任务不能认为已经可完整运行。
- 等待用户手动补充模板。

禁止事项：

- 不要删除缺失模板引用。
- 不要删除 `111.png`。
- 不要自动修改 `config/app_config.json` 删除模板引用。

### 自动抢拍当前阶段边界

- 自动抢拍需求已于 2026-05-06 正式下发。
- 当前已完成任务类型结构、配置结构、UI 工作台拆分、单窗口状态机框架、窗口任务队列、开始全部按队列顺序调度和测试。
- 主界面已按验收要求移除方案层级入口，运行视角固定为"窗口 -> 任务队列"。
- 选中自动抢拍任务时，中间主工作区只显示自动抢拍配置，右侧只显示辅助状态和日志。
- 自动抢拍已增加"进入拍卖界面"阶段，但尚未用真实窗口验证模板、区域和点击位置。
- UI 用户可见文案要求中文化，后续新增控件不要直接显示英文配置 key。
- 当前尚未做单窗口真实抢拍联调。
- 当前尚未做自动抢拍多窗口多进程并发；本阶段只做现有线程式多窗口调度，并在同一窗口内顺序执行任务队列。
- 当前尚未做 OCR 价格识别。
- 当前尚未做真实抢拍压测。
- 下一步应从单窗口真实联调开始，确认拍卖入口模板、拍卖界面确认模板、目标模板、识别区域、按钮偏移和点击行为。

### UI 重构未在 Windows 验证（新增 2026-05-07）

- UI 已重构：QTabWidget 拆分、方案控件移除、标题和标签格式调整。
- 但运行在 Linux 沙箱，无法运行 Windows GUI 程序。
- UI 效果和交互逻辑需在 Windows 上实际验证。
- 待人工处理：
  1. 运行 `py -3.14-32 D:\Ai\codex\H5\main.py` 查看效果。
  2. 确认 Tab 切换和布局正确。
  3. 更新 UI 截图。

### EXE 未重新打包（新增 2026-05-07）

- 本轮代码修改后尚未重新打包。
- 最近一次打包通过是 