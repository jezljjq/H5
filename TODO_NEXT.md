# TODO_NEXT

更新时间：2026-05-06

## 当前状态

- 当前不要开始自动抢拍任务，先补齐默认任务运行前置条件。
- `config/app_config.json` 当前引用 32 个唯一模板。
- `assets/templates` 当前实际只有：
  - `神界大陆.bmp`
  - `111.png`
- 其中 `神界大陆.bmp` 被配置引用，`111.png` 未被配置引用。
- 当前默认任务模板不完整，不能认为默认任务已经可完整运行。
- 缺失模板需要后续通过截图裁剪模板或导入旧模板补齐。
- 不要删除缺失引用。
- 不要删除 `111.png`。
- 不要自动改 `app_config.json` 删除这些模板引用。

## P0-1：补齐或确认缺失模板

目标：让默认任务引用的模板在 `assets/templates` 中真实存在。

待处理：

- 逐项确认 `app_config.json` 中缺失模板是否仍需要保留。
- 通过截图裁剪模板补齐缺失图片，或从旧模板/旧项目中导入。
- 保留缺失引用，直到模板补齐或用户明确确认删除引用。
- 保留未引用模板 `111.png`，等待人工确认用途。

## P0-2：确认测试识别和正式运行识别链路一致

目标：避免“测试当前步骤识别”能过，但正式运行走另一条链路失败。

待处理：

- 对比测试识别入口和正式运行入口使用的模板路径、ROI、阈值、重试参数。
- 确认两条链路都使用右侧“全局识别参数”中的默认识别阈值和默认重试次数。
- 确认当前 UI 不显示、不编辑单步阈值和单步重试。
- 不要把单步阈值和单步重试重新加回 UI。

## P1：开始自动抢拍单窗口闭环

前置条件：

- P0-1 缺失模板已补齐或明确确认。
- P0-2 测试识别和正式运行识别链路一致性已确认。

目标：

- 先做单窗口闭环。
- 只在单窗口链路稳定后，再考虑并发。

## P2：自动抢拍多窗口并发

前置条件：

- 单窗口自动抢拍闭环稳定。
- 已确认大漠绑定、后台识图、后台点击在多窗口下的稳定性和性能边界。

目标：

- 再推进多窗口并发调度。
- 需要关注并发数量、日志节流、窗口失效处理和任务停止清理。
