# TODO_NEXT

更新时间：2026-05-06

## 当前状态

- 自动抢拍需求已于 2026-05-06 正式下发。
- 当前阶段只做自动抢拍任务框架、配置结构、UI 工作台拆分、单窗口状态机框架、对应测试和文档更新。
- 当前阶段不要做多窗口多进程并发、OCR 价格识别、真实抢拍压测。
- 目录整理已完成，本阶段不要继续清理或移动 `build` / `dist` / `_cleanup_backup`。
- Git 上传因本机无法连接 `github.com:443` 暂时跳过，不要强行上传或 force push。
- 基础能力阶段已完成：
  - 识别链路一致性。
  - ROI 可视化。
  - 运行前配置检查。
- 模板缺失问题暂时跳过，等待用户晚上手动补充模板。
- `config/app_config.json` 当前引用 32 个唯一模板。
- `assets/templates` 当前实际只有：
  - `神界大陆.bmp`
  - `111.png`
- 其中 `神界大陆.bmp` 被配置引用，`111.png` 未被配置引用。
- 当前默认任务模板不完整，不能认为默认任务已经可完整运行。
- 缺失模板需要后续通过截图裁剪模板或导入旧模板补齐。
- 不要删除缺失引用。
- 不要删除 `111.png`。
- 不要自动改 `app_config.json` 删除这些模板引用。
- 最近验证状态：
  - 单元测试：`Ran 70 tests`，`OK`。
  - 编译检查：通过。
  - 打包：通过。
  - 最终 EXE：`D:\Ai\codex\H5\dist\全自动辅助助手\全自动辅助助手.exe`。
- 后续每个阶段完成后必须更新 `PROJECT_HANDOFF.md` 和 `TODO_NEXT.md`。
- 如果修改代码，必须记录新增/修改/删除或移动文件、完成事项、未完成事项、下一步建议、测试/编译/打包状态。

## P0-1：补齐或确认缺失模板（暂时跳过）

目标：让默认任务引用的模板在 `assets/templates` 中真实存在。

当前状态：用户会晚上手动补充模板，本阶段不处理模板补齐。

待处理：

- 逐项确认 `app_config.json` 中缺失模板是否仍需要保留。
- 通过截图裁剪模板补齐缺失图片，或从旧模板/旧项目中导入。
- 保留缺失引用，直到模板补齐或用户明确确认删除引用。
- 保留未引用模板 `111.png`，等待人工确认用途。

## P0-2：确认测试识别和正式运行识别链路一致（已完成）

目标：避免“测试当前步骤识别”能过，但正式运行走另一条链路失败。

完成状态：

- 测试识别和正式运行统一走 `h5bot.recognition.recognize_step()`。
- 两条链路都使用右侧“全局识别参数”中的默认识别阈值和默认重试次数。
- 当前 UI 不显示、不编辑单步阈值和单步重试。
- 不要把单步阈值和单步重试重新加回 UI。

## P0-3：ROI 可视化（已完成）

目标：右侧当前步骤详情可以选择、自动生成、清空 ROI。

完成状态：

- 选择 ROI 区域使用当前选中窗口截图，保存窗口客户区坐标 `x1,y1,x2,y2`。
- 自动生成 ROI 使用当前步骤模板组走统一识别入口，未命中或模板缺失时只提示原因，不修改原 ROI。
- 清空 ROI 表示恢复全窗口识别。
- 保存 ROI 后同步流程表、右侧详情、配置对象和 `config/app_config.json`。

## P0-4：运行前配置检查（已完成）

目标：开始运行前先发现明显配置问题，但不自动修改配置。

完成状态：

- 检查窗口、任务、启用步骤、ROI、跳转目标、模板文件、大漠可用性。
- 模板缺失只提示，不删除引用，不自动改 `app_config.json`。
- 大漠不可用提示将回退 OpenCV。

## P0-5：自动抢拍任务框架与单窗口状态机（已完成）

完成状态：

- 新增 `AuctionTaskConfig` 自动抢拍配置结构。
- 普通流程任务继续使用 `FlowRunner`，自动抢拍任务使用独立 `AuctionRunner` 框架。
- 新增任务类型字段，支持 `普通流程任务` / `自动抢拍任务`。
- UI 增加自动抢拍任务入口，并按任务类型拆分为普通流程工作台和自动抢拍工作台。
- 自动抢拍配置面板已移到中间主工作区，右侧仅保留全局识别参数、自动抢拍运行状态和运行日志。
- 自动抢拍增加“进入拍卖界面”配置区和状态机阶段。
- 自动抢拍配置新增拍卖入口图标模板组、拍卖界面确认模板组、拍卖入口识别区域、拍卖界面确认区域。
- 支持配置目标物品、灰色按钮、可点击按钮、确认按钮模板组。
- 支持拍卖列表识别区域、确认按钮识别区域、按钮偏移、频率滚动参数、运行策略。
- 支持单窗口启动自动抢拍和停止当前任务。
- 单窗口状态机已覆盖 S0 绑定窗口、S1 进入拍卖界面、S2 预扫描、S3 锁定按钮区域、S4 等待按钮可点击、S5 点击一口价、S6 点击确认、S7 继续扫描或结束。
- 用户可见 UI 文案已中文化，不在界面显示英文配置字段名。
- 第一版只做模板识别，不做 OCR，不做颜色判断，不做多窗口并发。
- 自动抢拍测试已覆盖默认值、序列化、按钮 ROI、灰色按钮不点击、可点击按钮点击、找不到目标滚动、最大滚动结束、配置不完整不崩溃、普通流程任务不受影响。

## P0-6：窗口级任务绑定与混合调度（已调整为 P0-7 队列模型）

完成状态：

- 窗口绑定已升级为窗口自己的属性，包含方案名、任务名、任务类型。
- 旧配置中的二元绑定会自动补为普通流程任务，兼容现有配置。
- 批量分配当前任务会把当前选中任务的任务类型一起保存。
- 左侧窗口列表显示窗口标题、绑定任务、任务类型和窗口状态。
- 开始全部按每个窗口绑定的任务类型选择执行器：
  - 普通流程任务使用 `FlowRunner`。
  - 自动抢拍任务使用 `AuctionRunner`。
- 普通流程任务和自动抢拍任务可以在同一次开始全部中混合运行。
- 同一窗口增加运行互斥保护，重复启动会提示先停止当前任务。
- 窗口状态按 hwnd 独立维护，一个窗口异常不会把其他窗口标记为异常。
- 日志统一带窗口标题和任务类型前缀。

## P0-7：窗口任务队列模型与顺序调度（已完成）

完成状态：

- 运行关系已调整为 `窗口 -> 任务队列`，不再是 `窗口 -> 方案 -> 固定任务`。
- 每个窗口保存自己的任务队列，队列项包含方案名、任务名、任务类型、启用状态、顺序和预留执行策略字段。
- 方案保留为任务模板分类、预设集合和快速导入来源，不再作为窗口运行时强制层级。
- 第一版队列项使用任务模板引用，修改模板会影响引用该模板的窗口队列；窗口独立副本留到后续。
- UI 中间主区域增加当前窗口任务队列：
  - 左侧显示“可选任务（模板库）”，只展示任务名和任务类型，不展示方案层级。
  - 右侧显示当前窗口任务队列。
  - 添加到当前窗口。
  - 批量添加到选中窗口。
  - 复制当前窗口队列到选中窗口。
  - 替换选中窗口队列。
  - 从队列移除。
  - 上移任务。
  - 下移任务。
  - 启用/禁用任务。
  - 清空任务队列。
  - 批量清空选中窗口队列。
- 左侧窗口列表显示窗口标题、队列数量、当前执行任务和状态。
- 主界面已移除方案下拉框、新增方案、重命名方案、删除方案等方案层级入口；方案只保留为内部兼容数据和任务模板来源。
- 当前窗口任务队列中选中的任务会驱动配置区，普通流程和自动抢拍不会混在一起显示。
- 选中自动抢拍任务时，中间主工作区只显示自动抢拍配置，隐藏普通流程路径、普通流程步骤表和普通流程当前步骤详情。
- 自动抢拍右侧状态区增加配置完整性提示。
- 开始全部按每个窗口自己的启用队列顺序执行。
- 同一窗口内任务顺序执行，不同窗口之间仍按现有线程式调度并行。
- 普通流程任务和自动抢拍任务可以共存在同一个窗口队列中。
- 旧 `window_task_bindings` 会迁移为对应窗口任务队列中的第一个任务，不丢失旧绑定关系。
- 未做自动抢拍多进程并发、OCR、模板补齐和 Git 上传。

## 下一步建议

下一个工具接手时，优先从 `P1：自动抢拍单窗口真实联调` 开始；模板缺失仍等待用户手动补充，不能因为缺失模板删除配置引用。

## P1：开始自动抢拍单窗口闭环

前置条件：

- 自动抢拍任务框架已完成。
- 用户补齐或确认自动抢拍所需模板。
- 真实窗口可绑定并可截图/识图。

目标：

- 做单窗口真实联调。
- 确认目标物品模板、按钮模板、确认模板和 ROI。
- 验证 S0-S7 日志、点击位置、等待间隔和停止策略。
- 只在单窗口链路稳定后，再考虑并发。

## P2：自动抢拍多窗口并发

前置条件：

- 单窗口自动抢拍闭环稳定。
- 已确认大漠绑定、后台识图、后台点击在多窗口下的稳定性和性能边界。

目标：

- 再推进多窗口并发调度。
- 需要关注并发数量、日志节流、窗口失效处理和任务停止清理。
